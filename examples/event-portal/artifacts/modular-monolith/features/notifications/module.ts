import { PortalModule } from "../../runtime/types";

export const portalModule: PortalModule = {
  id: "feature.event.notifications",
  render: () => "<section id=\"notifications\"><h2>Notifications</h2><p>Schedule announcements enabled.</p></section>",
};
