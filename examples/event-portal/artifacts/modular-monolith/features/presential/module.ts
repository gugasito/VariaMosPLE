import { PortalModule } from "../../runtime/types";

export const portalModule: PortalModule = {
  id: "feature.event.presential",
  render: () => "<section id=\"presential\"><h2>In-person mode</h2><p>Access to the main auditorium.</p></section>",
};
